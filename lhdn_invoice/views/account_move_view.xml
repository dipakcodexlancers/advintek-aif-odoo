<odoo>

    <!-- Button -->
    <record id="view_move_form_inherit_lhdn" model="ir.ui.view">
        <field name="name">account.move.lhdn.button</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">

            <xpath expr="//header" position="inside">
                <button name="action_submit_irbm"
                        string="IRBM Submission"
                        type="object"
                        class="btn-primary"
                        invisible="state != 'posted'"/>
            </xpath>

        </field>
    </record>

    <!-- Tab -->
    <record id="view_move_form_inherit_lhdn_tab" model="ir.ui.view">
        <field name="name">account.move.lhdn.tab</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">

            <xpath expr="//page[@name='other_info']" position="after">
                <page string="IRBM Submission Details">

                    <group>
                        <group>
                            <field name="lhdn_status"/>
                            <field name="lhdn_uuid"/>
                            <field name="lhdn_validation_date"/>
                        </group>

                        <group>
                            <field name="lhdn_validation_link"/>
                            <field name="lhdn_rejection_result"/>
                        </group>
                    </group>

                </page>
            </xpath>

        </field>
    </record>

</odoo>